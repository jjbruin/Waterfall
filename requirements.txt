# Web framework
Flask>=3.1
flask-cors>=6.0
flask-caching>=2.3
Werkzeug>=3.1

# Database
# <2.1: SQLAlchemy 2.1 (released Sep 24 2026) makes a bare postgresql:// URL load
# psycopg (v3), not psycopg2. v524 built with it and every worker died on boot with
# "No module named psycopg". Moving to 2.1 is a deliberate upgrade, not a rebuild.
SQLAlchemy>=2.0,<2.1
psycopg2-binary>=2.9
pyodbc>=5.3

# Auth
PyJWT>=2.11
Authlib>=1.6
python-dotenv>=1.2

# Data & computation
pandas>=2.3
numpy>=2.4
scipy>=1.17
openpyxl>=3.1
pdfplumber>=0.11
PyMuPDF>=1.25

# HTTP / email
requests>=2.32
# Azure Communication Services email. The SDK rather than raw REST
# because ACS authenticates with an HMAC-signed request, and a signing
# bug fails as a bare 401 -- indistinguishable from a bad key.
azure-communication-email>=1.0

# AI assistant
anthropic>=0.109
